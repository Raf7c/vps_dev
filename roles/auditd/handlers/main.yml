---
# Sur Fedora/RHEL, auditd refuse systemctl restart (géré par le kernel).
- name: Restart auditd
  ansible.builtin.command: service auditd restart
  changed_when: true

- name: Reload auditd rules
  ansible.builtin.command: augenrules --load
  changed_when: true
